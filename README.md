# vs-asp-classic README

Basic VSCode language support for ASP Classic pages supporting `<%` `%>` (VBScript only) and `<script runat=server language=(jscript|javascript|vbscript)>` tags for both VBScript/Javascript